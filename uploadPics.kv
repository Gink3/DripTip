#:kivy 2.0.0
Screen:
    MDBoxLayout:
        orientation:'vertical'
        spacing: 20
        MDToolbar:
            title: "Jake Dang"
            type: "top"
            right_action_items: [["message", lambda x: app.callback()], ["menu", lambda x: app.callback()]]
        MDBoxLayout:
            orientation: 'horizontal'
            MDLabel:
            MDRaisedButton:
                text: "start"
                halign: "center"
                md_bg_color: 0,0,0,0
            MDLabel:
        MDLabel:
    
        
        